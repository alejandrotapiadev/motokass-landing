---
import MainLayout from "../layouts/MainLayout.astro";
---

<MainLayout title="Sección en construcción" description="La sección que intentas acceder está en construcción.">
  <section class="under-construction">
    <div class="uc-card">
      <div class="uc-icon">🏗️</div>
      <h1>Sección en construcción</h1>
      <p>
        La página a la que intentas acceder está actualmente en desarrollo.
        Estamos trabajando para ofrecerte la mejor experiencia posible.
      </p>

      <a href="/" class="uc-button">
        Volver al inicio
      </a>
    </div>
  </section>
</MainLayout>

<style>
.under-construction {
  min-height: 100vh;
  background: linear-gradient(
    135deg,
    var(--color-primary-dark),
    var(--color-primary)
  );
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 2rem;
}

.uc-card {
  background: var(--color-white);
  padding: 3rem 2.5rem;
  border-radius: 18px;
  text-align: center;
  max-width: 500px;
  box-shadow: 0 25px 50px rgba(0, 0, 0, 0.25);
  animation: fadeIn 0.6s ease-out;
}

.uc-icon {
  font-size: 3rem;
  margin-bottom: 1rem;
}

.uc-card h1 {
  font-size: 1.8rem;
  margin-bottom: 1rem;
  color: var(--color-primary-dark);
}

.uc-card p {
  color: var(--color-gray-dark);
  line-height: 1.6;
  margin-bottom: 2rem;
}

.uc-button {
  display: inline-block;
  padding: 0.8rem 1.8rem;
  background: var(--color-primary);
  color: var(--color-white);
  text-decoration: none;
  font-weight: 600;
  border-radius: 999px;
  transition: all 0.25s ease;
}

.uc-button:hover {
  background: var(--color-primary-light);
  transform: translateY(-3px);
  box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
}

@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
</style>
